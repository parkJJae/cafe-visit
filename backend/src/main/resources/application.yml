spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cafe_log?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: qkrwogus1205!
  jpa:
    hibernate:
      ddl-auto: update   # ???: create / update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
